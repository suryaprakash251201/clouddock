// Single source of truth for the displayed app version.

/// Keep in sync with `version:` in pubspec.yaml.
const String appVersion = '1.0.1';

const String appVersionLabel = 'CloudDock v$appVersion';
